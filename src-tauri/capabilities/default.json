{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for CatCatDM",
  "windows": ["main", "dm", "plugin", "yin", "live-preview"],
  "permissions": [
    "core:default",
    "core:event:allow-emit",
    "core:event:allow-listen",
    "core:window:default"
  ]
}
